export enum MissionVisionTypeEnum {
  Home = 'in home',
  AboutUs = 'in about us',
}

export enum LocaleValueEnum {
  En = 'en',
  Ru = 'ru',
}

export enum AuthorsInPageEnum {
  Home = 'home',
  ABoutUs = 'about us',
}

export enum TextInAboutTeamEnum {
  Right = 'text right',
  Left = 'text left',
}

export enum HeaderTypeEnum {
  Header = 'header',
  Footer = 'footer',
}

export enum StatusCodeEnum {
  Ok = 'OK',
}

export enum TitleAlignEnum {
  Center = 'center',
  Left = 'left',
}
